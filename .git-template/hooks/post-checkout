#!/bin/sh
# Git copies template hooks before checkout. The checked-out script does the work.
[ "$3" = 1 ] || exit 0
root=$(git rev-parse --show-toplevel 2>/dev/null) || exit 0
[ -f "$root/.githooks/post-checkout" ] || exit 0
sh "$root/.githooks/post-checkout" "$@"
exit 0
